# Application Configuration

# Root directory to serve (absolute path on host, will be mounted as /data in container)
ROOT_DIRECTORY=/path/to/your/image/directory

# Backend API port
PORT=8765

# Frontend port (when running locally, Docker maps to port 80 internally)
FRONTEND_PORT=5173

# API base URL (used by frontend to connect to backend)
VITE_API_BASE_URL=http://localhost:8765

# Node environment
NODE_ENV=development
